function LogementLocation({ title, location }) {
    return (
        <>
            <div>
                <h2>{title}</h2>
                <span>{location}</span>
            </div>
        </>
    )
}

export default LogementLocation
